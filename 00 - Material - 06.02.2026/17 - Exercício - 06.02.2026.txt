17)

#include <iostream>
#include <locale>
#include <iomanip>

using namespace std;

int main(){

    setlocale(LC_ALL, "portuguese");
    int Valor = 0;

    cout << "Entre com um valor inteiro entre 0 e 10: ";
    cin >> Valor;

    if(Valor < 0 || Valor > 10){
        do{
            cout << "Entre com um valor válido: ";
            cin >> Valor;
        }while(Valor < 0 || Valor > 10);
    }
    cout << endl;
    cout << "Valor setado corretamente: " << Valor << ".";
}
