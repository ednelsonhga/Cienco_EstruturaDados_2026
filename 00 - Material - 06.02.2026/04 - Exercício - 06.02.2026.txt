4)

#include <iostream>
#include <locale>
#include <iomanip>

using namespace std;

int main(){

    setlocale(LC_ALL, "portuguese");
    std::cout << std::fixed << std::setprecision(2);
    float SalarioFuncionario, Reajuste, ValorReajuste, SalarioFinal;

    cout << "Cálculo de Reajuste" << endl;
    cout << endl;
    cout << "Entre com seu salário: R$ ";
    cin >> SalarioFuncionario;

    if(SalarioFuncionario < 1000){
        Reajuste = 0.15;
    }else if (SalarioFuncionario <= 1500){
        Reajuste = 0.10;
    }else{
        Reajuste = 0.05;
    }

    ValorReajuste = SalarioFuncionario * Reajuste;
    SalarioFinal  = SalarioFuncionario + ValorReajuste;

    cout << "Seu reajuste será de: R$ " << ValorReajuste << endl;
    cout << "Salário final: R$ " << SalarioFinal << endl;

}